import { colors } from "./tokens"

export const lightTheme = {
    colors: {
        text: colors.grey[800],
        bg: colors.grey[50],
    },
};

export const darkTheme = {
    colors: {
        text: colors.grey[50],
        bg: colors.grey[800],
    },
};